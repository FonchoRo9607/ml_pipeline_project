# Core scientific stack
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Visualización
matplotlib>=3.8.0
seaborn>=0.12.2

# Generación de informes HTML/PDF
weasyprint>=60.0  # Para PDF desde HTML (requiere instalación de dependencias del sistema)
jinja2>=3.1.2     # Para plantillas HTML si usas reportes personalizados

# Trazabilidad y logs
loguru>=0.7.0

# Automatización y orquestación
python-dotenv>=1.0.0  # Para manejar variables de entorno
tqdm>=4.66.0          # Para barras de progreso

# Opcional: versionado de experimentos
mlflow>=2.9.0         # Si decides integrar seguimiento de modelos
dvc>=3.30.0           # Alternativa para versionado de datos y modelos

# Opcional: API para servir predicciones
fastapi>=0.110.0
uvicorn>=0.27.0